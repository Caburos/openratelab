node_modules
dist
dist-ssr

# Netlify CLI local state/build artifacts — never commit these
.netlify
*.local
.DS_Store
.env.local
.env.production

# Keep .env (it only has placeholder comments, no secrets)

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Data files (contact submissions stored locally in dev)
data/contacts.json

# Claude — keep project skills/agents/settings tracked so they travel with
# the repo across sessions; only exclude genuinely local-only state.
.claude/settings.local.json

# Planning/working docs — kept locally for reference, not published to the
# public repo (repo should only contain the actual site and its content).
ASTRO_MIGRATION_PLAN.md
UROSBUILDS_COMPARISON_TRANSCRIPT.md
